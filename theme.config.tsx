import React from "react";
import { DocsThemeConfig } from "nextra-theme-docs";

const config: DocsThemeConfig = {
  logo: <span>--terminal-commands</span>,
  project: {
    link: "https://github.com/sudo-adduser-jordan/terminal-commands",
  },
  chat: {
    link: "https://discord.com",
  },
  docsRepositoryBase:
    "https://github.com/sudo-adduser-jordan/terminal-commands",
  footer: {
    text: "--terminal-commands",
  },
};

export default config;
