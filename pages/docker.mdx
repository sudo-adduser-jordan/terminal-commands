```bash copy
docker run -it -p 3000:3000 debian bash
```

```bash copy
docker run -it --network host debian bash
```

```bash copy
docker exec -it  bash
```

```bash copy
docker ps
```

```bash copy
 docker cp .\starting_point_lostcoast.ovpn 9ee01c27c697:/
```

```bash copy
docker run `
    -it `
    --device=/dev/net/tun `
    --cap-add=net_admin `
    --sysctl net.ipv6.conf.all.disable_ipv6=0 `
    debian bash
```
